# Typo Payments Demo - Environment Variables
# These are intentionally insecure values for demonstration purposes

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=1

# PostgreSQL Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_NAME=typo_payments
DB_USER=typo_admin
DB_PASSWORD=insecure_password_123

# For local development (outside Docker), use:
# DB_HOST=localhost
